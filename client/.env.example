# Base URL for fetching models and assets. 
# Leave empty or set to / in production if same origin.
VITE_API_BASE_URL=http://localhost:5000

# API URL for fetching and saving settings.
# Leave as /api if you are proxying via Vite or serving directly.
VITE_API_URL=http://localhost:5000/api
